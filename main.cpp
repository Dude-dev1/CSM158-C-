#include <iostream>
using namespace std;

int main() {

    int factor;
    cout << "Enter the value of the factor here ";
    cin >> factor;

    cout << ( factor == 2 ? "The number is prime" : "The number is not prime");

    return 0;
}
